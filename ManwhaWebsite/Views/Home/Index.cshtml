@model ManwhaWebsite.Models.HomeViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ManhwaVault — Discover Your Next Obsession</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:wght@300;400;500;600&family=Noto+Serif+KR:wght@400;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/home.css" asp-append-version="true" />
</head>
<body>

    <!-- NAV -->
    <nav class="nav">
        <div class="nav-inner">
            <a href="#" class="nav-logo">Manhwa<span>Vault</span></a>
            <div class="nav-search">
                <span class="nav-search-icon">⌕</span>
                <input type="text" placeholder="Search titles, genres, authors..." />
            </div>
            <button class="nav-search-toggle" aria-label="Search" onclick="toggleMobileSearch()">
                <svg xmlns="http://www.w3.org/2000/svg" width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
            </button>
            <div class="nav-search-overlay" id="navSearchOverlay">
                <div class="nav-search-overlay-inner">
                    <input id="mobileSearchInput" type="text" placeholder="Search titles, genres, authors..." />
                    <button class="nav-search-close" onclick="toggleMobileSearch()" aria-label="Close search">✕</button>
                </div>
            </div>
            <div class="nav-actions">
                <div class="nav-links">
                    <div><a href="#">Browse</a></div>
                    <div><a href="#">Genres</a></div>
                    <div><a href="#">Rankings</a></div>
                    <div><a href="#">Latest</a></div>
                </div>
                <div class="nav-right">
                    <a href="#" class="btn-login">Sign In</a>
                    <a href="#" class="btn-signup">Join Free</a>
                    <button class="nav-hamburger" onclick="toggleDrawer()">☰</button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Mobile Drawer -->
    <div class="nav-drawer" id="navDrawer" onclick="toggleDrawer()">
        <div class="nav-drawer-panel" onclick="event.stopPropagation()">
            <div style="font-family:'Bebas Neue',sans-serif;font-size:22px;letter-spacing:2px;margin-bottom:12px;">
                Manhwa<span style="color:var(--accent)">Vault</span>
            </div>
            <a href="#">Browse</a>
            <a href="#">Genres</a>
            <a href="#">Rankings</a>
            <a href="#">Latest</a>
            <hr style="border-color:var(--border);margin:8px 0" />
            <a href="#">Sign In</a>
            <a href="#">Join Free</a>
        </div>
    </div>

    <!-- ══════════════════════════════════════════
         HERO — from Model.Trending
    ══════════════════════════════════════════ -->
    <section class="hero">
        <div class="hero-slides" id="heroSlides">
            @{
                int heroIdx = 0;
            }
            @foreach (var item in Model.Trending)
            {
                var bgUrl = !string.IsNullOrEmpty(item.BannerImageUrl) ? item.BannerImageUrl : item.CoverImageUrl;
                <div class="hero-slide @(heroIdx == 0 ? "active" : "")" data-index="@heroIdx">
                    <div class="hero-slide-bg hero-slide-bg--blurred" style="background-image:url('@bgUrl')"></div>
                    <div class="hero-slide-cover-wrap">
                        <img src="@item.CoverImageUrl" alt="@item.Title" loading="eager" />
                    </div>
                    <div class="hero-slide-overlay"></div>
                    <div class="hero-content">
                        <div class="container">
                            <h1 class="hero-title">@item.Title</h1>
                            <div class="hero-meta">
                                <span class="hero-rank">#@(heroIdx + 1) Trending</span>
                            </div>
                            <p class="hero-desc">@item.Description</p>
                            <div class="hero-stats">
                                <div class="hero-stat" style="color:var(--gold)">★ @item.Rating.ToString("F1")</div>
                                <div class="hero-stat">👁 <strong>@FormatNum(item.Popularity)</strong> readers</div>
                                <div class="hero-stat">🕐 <strong>@item.LastUpdated.ToString("MMM dd")</strong></div>
                            </div>
                            <div class="hero-actions">
                                <a href="https://anilist.co/manga/@item.Id" target="_blank" class="btn-hero-primary">▶ View Details</a>
                                <a href="https://anilist.co/manga/@item.Id" target="_blank" class="btn-hero-secondary">+ Add to List</a>
                            </div>
                        </div>
                    </div>
                </div>
                heroIdx++;
            }
        </div>

        <!-- Thumb strip -->
        <div class="hero-thumbs">
            @{
                int thumbIdx = 0;
            }
            @foreach (var item in Model.Trending)
            {
                <div class="hero-thumb @(thumbIdx == 0 ? "active" : "")" onclick="goToSlide(@thumbIdx)">
                    <img src="@item.CoverImageUrl" alt="@item.Title" loading="lazy" />
                </div>
                thumbIdx++;
            }
        </div>

        <!-- Dots -->
        <div class="hero-dots">
            @for (int d = 0; d < Model.Trending.Count(); d++)
            {
                <div class="hero-dot @(d == 0 ? "active" : "")" onclick="goToSlide(@d)"></div>
            }
        </div>
    </section>

    <!-- GENRE PILLS -->
    <div class="genres-band">
        <div class="genres-scroll">
            <a href="#" class="genre-pill active">All</a>
            <a href="#" class="genre-pill">Action</a>
            <a href="#" class="genre-pill">Romance</a>
            <a href="#" class="genre-pill">Fantasy</a>
            <a href="#" class="genre-pill">System</a>
            <a href="#" class="genre-pill">Regression</a>
            <a href="#" class="genre-pill">Isekai</a>
            <a href="#" class="genre-pill">Martial Arts</a>
            <a href="#" class="genre-pill">Horror</a>
            <a href="#" class="genre-pill">Comedy</a>
            <a href="#" class="genre-pill">Drama</a>
            <a href="#" class="genre-pill">Cultivation</a>
            <a href="#" class="genre-pill">Thriller</a>
            <a href="#" class="genre-pill">Historical</a>
            <a href="#" class="genre-pill">Slice of Life</a>
        </div>
    </div>

    <!-- ══════════════════════════════════════════
         POPULAR — from Model.Popular
    ══════════════════════════════════════════ -->
    <section class="section-popular">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">🔥 <span>Popular</span></h2>
                <a href="#" class="view-all">View All →</a>
            </div>
            <div class="scroll-row-wrapper">
                <button class="scroll-btn scroll-btn-left" onclick="scrollRow('popularRow',-1)">‹</button>
                <div class="scroll-row" id="popularRow" data-base-count="15">
                    @{
                        int popIdx = 0;
                    }
                    @foreach (var item in Model.Popular)
                    {
                        <div class="card">
                            <div class="card-cover">
                                <img src="@item.CoverImageUrl" alt="@item.Title" loading="lazy" />
                                <div class="card-overlay">
                                    <a href="https://anilist.co/manga/@item.Id" target="_blank" class="card-overlay-read">▶ View Details</a>
                                </div>
                            </div>
                            <div class="card-info">
                                <div class="card-title">@item.Title</div>
                                <div class="card-meta">
                                    <span class="card-rating">@item.Rating.ToString("F1")</span>
                                    <span class="card-views">@FormatNum(item.Popularity)</span>
                                </div>
                            </div>
                        </div>
                        popIdx++;
                    }
                </div>
                <button class="scroll-btn scroll-btn-right" onclick="scrollRow('popularRow',1)">›</button>
            </div>
        </div>
    </section>

    <!-- ══════════════════════════════════════════
         NEWLY UPDATED — from Model.NewlyUpdated
    ══════════════════════════════════════════ -->
    <section class="section-updated">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">🆕 <span>Newly</span> Updated</h2>
                <a href="#" class="view-all">View All →</a>
            </div>
            <div class="updated-grid" data-base-count="9">
                @foreach (var item in Model.NewlyUpdated)
                {
                    var isNew = (DateTime.UtcNow - item.LastUpdated).TotalHours < 24;
                    <a href="https://anilist.co/manga/@item.Id" target="_blank" class="updated-card">
                        <div class="updated-cover">
                            <img src="@item.CoverImageUrl" alt="@item.Title" loading="lazy" />
                            @if (isNew)
                            {
                                <div class="updated-cover-badge"><span class="badge badge-new">New</span></div>
                            }
                        </div>
                        <div class="updated-info">
                            <div class="updated-title">@item.Title</div>
                            <div class="updated-chapter">@(item.LatestChapter ?? item.Status)</div>
                            <div class="updated-time">@GetTimeAgo(item.LastUpdated)</div>
                        </div>
                    </a>
                }
            </div>
        </div>
    </section>

    <!-- ══════════════════════════════════════════
         DISCOVER — from Model.Random
    ══════════════════════════════════════════ -->
    <section class="section-discover">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">✨ <span>Discover</span> Something New</h2>
                <div style="display:flex;gap:10px;align-items:center;">
                    <a href="#" class="view-all">Randomize →</a>
                    <button class="btn-discover-filter" id="filterToggleBtn" onclick="toggleDiscoverFilter()">⚙ Filter</button>
                </div>
            </div>

            <div class="filter-backdrop" id="filterBackdrop" onclick="toggleDiscoverFilter()"></div>

            <div class="discover-filter-panel" id="discoverFilter">
                <div class="filter-drag-handle"></div>
                <div class="filter-section filter-section--full">
                    <div class="filter-label">Genre</div>
                    <div class="filter-genres" id="filterGenresList"></div>
                </div>
                <div class="filter-section">
                    <div class="filter-label">Status</div>
                    <div class="filter-status-btns">
                        <button class="filter-status-btn active" data-status="all" onclick="setFilterStatus(this)">All</button>
                        <button class="filter-status-btn" data-status="completed" onclick="setFilterStatus(this)">Completed</button>
                        <button class="filter-status-btn" data-status="ongoing" onclick="setFilterStatus(this)">Ongoing</button>
                    </div>
                </div>
                <div class="filter-section">
                    <div class="filter-label">Min Rating: <span id="filterRatingDisplay">0</span></div>
                    <input type="range" class="filter-range" id="filterRatingRange" min="0" max="10" step="0.5" value="0"
                           oninput="document.getElementById('filterRatingDisplay').textContent=parseFloat(this.value).toFixed(1); applyDiscoverFilter()" />
                </div>
                <div class="filter-section">
                    <div class="filter-label">Chapters</div>
                    <div class="filter-chapters-row">
                        <input type="number" class="filter-num" id="filterChMin" placeholder="Min" min="0" oninput="applyDiscoverFilter()" />
                        <span class="filter-sep">–</span>
                        <input type="number" class="filter-num" id="filterChMax" placeholder="Max" min="0" oninput="applyDiscoverFilter()" />
                    </div>
                </div>
                <div class="filter-section">
                    <div class="filter-label">&nbsp;</div>
                    <button class="filter-reset" onclick="resetDiscoverFilter()">↺ Reset filters</button>
                </div>
            </div>

            <div class="discover-grid" data-base-count="6">
                @foreach (var item in Model.Random)
                {
                    <a href="https://anilist.co/manga/@item.Id" target="_blank" class="discover-card"
                       data-genres="@string.Join("|", item.Genres)"
                       data-status="@item.Status"
                       data-rating="@item.Rating.ToString("F1", System.Globalization.CultureInfo.InvariantCulture)"
                       data-chapters="@(item.ChapterCount?.ToString() ?? "0")">
                        <div class="discover-cover">
                            <img src="@item.CoverImageUrl" alt="@item.Title" loading="lazy" />
                        </div>
                        <div class="discover-body">
                            <div class="discover-title">@item.Title</div>
                            <div class="discover-desc">@item.Description</div>
                            <div class="discover-footer">
                                <span class="discover-rating">@item.Rating.ToString("F1")</span>
                                <span class="discover-views">@FormatNum(item.Popularity) readers</span>
                            </div>
                        </div>
                    </a>
                }
            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <div class="container">
            <div class="footer-inner">
                <div class="footer-brand">
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:22px;letter-spacing:2px;">
                        Manhwa<span style="color:var(--accent)">Vault</span>
                    </div>
                    <p>Your ultimate destination for manhwa, manhua, and manga. Track your list, discover new titles, and connect with the community.</p>
                </div>
                <div class="footer-col"><h4>Browse</h4><ul><li><a href="#">Popular</a></li><li><a href="#">Latest</a></li><li><a href="#">Rankings</a></li><li><a href="#">Genres</a></li></ul></div>
                <div class="footer-col"><h4>Account</h4><ul><li><a href="#">Sign Up</a></li><li><a href="#">Login</a></li><li><a href="#">Reading List</a></li><li><a href="#">Settings</a></li></ul></div>
                <div class="footer-col"><h4>Info</h4><ul><li><a href="#">About</a></li><li><a href="#">Contact</a></li><li><a href="#">Privacy</a></li><li><a href="#">Terms</a></li></ul></div>
            </div>
            <div class="footer-bottom">
                <span>© @DateTime.Now.Year ManhwaVault. All rights reserved.</span>
                <span>Made for readers, by readers.</span>
            </div>
        </div>
    </footer>

    <script>
        // ── HERO SLIDER ──────────────────────────────────────────────
        let currentSlide = 0;
        const slides = document.querySelectorAll('.hero-slide');
        const thumbs = document.querySelectorAll('.hero-thumb');
        const dots   = document.querySelectorAll('.hero-dot');
        let autoplayTimer;

        function goToSlide(idx) {
            slides[currentSlide].classList.remove('active');
            thumbs[currentSlide]?.classList.remove('active');
            dots[currentSlide]?.classList.remove('active');
            currentSlide = (idx + slides.length) % slides.length;
            slides[currentSlide].classList.add('active');
            thumbs[currentSlide]?.classList.add('active');
            dots[currentSlide]?.classList.add('active');
            resetAutoplay();
        }
        function resetAutoplay() {
            clearInterval(autoplayTimer);
            autoplayTimer = setInterval(() => goToSlide(currentSlide + 1), 5500);
        }
        document.addEventListener('keydown', e => {
            if (e.key === 'ArrowRight') goToSlide(currentSlide + 1);
            if (e.key === 'ArrowLeft')  goToSlide(currentSlide - 1);
        });
        (function () {
            let startX = 0;
            const hero = document.querySelector('.hero');
            hero.addEventListener('touchstart', e => startX = e.touches[0].clientX, { passive: true });
            hero.addEventListener('touchend', e => {
                const dx = e.changedTouches[0].clientX - startX;
                if (Math.abs(dx) > 50) goToSlide(currentSlide + (dx < 0 ? 1 : -1));
            });
        })();
        resetAutoplay();

        function scrollRow(id, dir) {
            document.getElementById(id)?.scrollBy({ left: dir * 480, behavior: 'smooth' });
        }

        function applyGridFill(gridEl) {
            if (!gridEl) return;
            const all = Array.from(gridEl.children);
            if (!all.length) return;
            // reset only items that aren't hidden by the filter
            all.forEach(el => { if (!el.dataset.filteredOut) el.style.display = ''; });
            const cols = getComputedStyle(gridEl).gridTemplateColumns.split(' ').length;
            if (cols <= 1) return;
            const pool = all.filter(el => !el.dataset.filteredOut);
            const base = parseInt(gridEl.dataset.baseCount) || pool.length;
            const ideal = Math.ceil(base / cols) * cols;
            const avail = Math.floor(pool.length / cols) * cols;
            const show = avail >= ideal ? ideal : (avail || pool.length);
            pool.forEach((el, i) => {
                el.style.display = i < show ? '' : 'none';
                if (i < show && el.style.opacity === '0') { el.style.opacity = '1'; el.style.transform = ''; }
            });
        }
        function applyGridFills() {
            applyGridFill(document.getElementById('popularRow'));
            applyGridFill(document.querySelector('.updated-grid'));
            applyGridFill(document.querySelector('.discover-grid'));
        }
        applyGridFills();
        let fillTimer;
        window.addEventListener('resize', () => {
            clearTimeout(fillTimer);
            fillTimer = setTimeout(() => {
                applyGridFills();
                if (window.innerWidth > 768) {
                    document.getElementById('filterBackdrop')?.classList.remove('visible');
                    document.body.style.overflow = '';
                }
            }, 100);
        });

        // ── DISCOVER FILTER ──────────────────────────────────────────
        function toggleDiscoverFilter() {
            const panel    = document.getElementById('discoverFilter');
            const btn      = document.getElementById('filterToggleBtn');
            const backdrop = document.getElementById('filterBackdrop');
            const open     = panel.classList.toggle('open');
            btn.classList.toggle('active', open);
            if (open && !document.getElementById('filterGenresList').children.length)
                buildGenrePills();
            if (window.innerWidth <= 768) {
                backdrop.classList.toggle('visible', open);
                document.body.style.overflow = open ? 'hidden' : '';
            }
        }

        function buildGenrePills() {
            const seen = new Set();
            document.querySelectorAll('.discover-card[data-genres]').forEach(card => {
                card.dataset.genres.split('|').filter(Boolean).forEach(g => seen.add(g));
            });
            const list = document.getElementById('filterGenresList');
            [...seen].sort().forEach(genre => {
                const btn = document.createElement('button');
                btn.className = 'filter-genre-pill';
                btn.textContent = genre;
                btn.onclick = () => { btn.classList.toggle('active'); applyDiscoverFilter(); };
                list.appendChild(btn);
            });
        }

        function setFilterStatus(btn) {
            document.querySelectorAll('.filter-status-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            applyDiscoverFilter();
        }

        function applyDiscoverFilter() {
            const activeGenres = [...document.querySelectorAll('.filter-genre-pill.active')].map(b => b.textContent);
            const status   = document.querySelector('.filter-status-btn.active')?.dataset.status ?? 'all';
            const minRating = parseFloat(document.getElementById('filterRatingRange').value) || 0;
            const minCh  = parseInt(document.getElementById('filterChMin').value) || 0;
            const maxChRaw = document.getElementById('filterChMax').value;
            const maxCh  = maxChRaw !== '' ? parseInt(maxChRaw) : Infinity;

            document.querySelectorAll('.discover-card').forEach(card => {
                const genres   = card.dataset.genres.split('|').filter(Boolean);
                const cardStatus = card.dataset.status ?? '';
                const rating   = parseFloat(card.dataset.rating) || 0;
                const chapters = parseInt(card.dataset.chapters) || 0;

                let show = true;
                if (activeGenres.length && !activeGenres.some(g => genres.includes(g))) show = false;
                if (status === 'completed' && cardStatus !== 'FINISHED') show = false;
                if (status === 'ongoing'   && cardStatus === 'FINISHED') show = false;
                if (rating > 0 && rating < minRating) show = false;
                if (minCh > 0 && chapters < minCh) show = false;
                if (maxCh !== Infinity && chapters > maxCh) show = false;

                if (show) {
                    delete card.dataset.filteredOut;
                    card.style.display = '';
                } else {
                    card.dataset.filteredOut = '1';
                    card.style.display = 'none';
                }
            });

            applyGridFill(document.querySelector('.discover-grid'));
        }

        function resetDiscoverFilter() {
            document.querySelectorAll('.filter-genre-pill.active').forEach(b => b.classList.remove('active'));
            document.querySelectorAll('.filter-status-btn').forEach(b => b.classList.remove('active'));
            document.querySelector('[data-status="all"]').classList.add('active');
            document.getElementById('filterRatingRange').value = 0;
            document.getElementById('filterRatingDisplay').textContent = '0.0';
            document.getElementById('filterChMin').value = '';
            document.getElementById('filterChMax').value = '';
            document.querySelectorAll('.discover-card').forEach(card => {
                delete card.dataset.filteredOut;
                card.style.display = '';
            });
            applyGridFills();
        }
        function toggleDrawer() {
            document.getElementById('navDrawer')?.classList.toggle('open');
        }
        function toggleMobileSearch() {
            const overlay = document.getElementById('navSearchOverlay');
            const isOpen = overlay.classList.toggle('is-open');
            if (isOpen) {
                document.getElementById('mobileSearchInput').focus();
            }
        }
        document.getElementById('navSearchOverlay')?.addEventListener('click', function(e) {
            if (e.target === this) toggleMobileSearch();
        });

        // Scroll reveal
        const observer = new IntersectionObserver(entries => {
            entries.forEach(e => {
                if (e.isIntersecting) {
                    e.target.style.opacity = '1';
                    e.target.style.transform = 'translateY(0)';
                    observer.unobserve(e.target);
                }
            });
        }, { threshold: 0.06 });
        document.querySelectorAll('.updated-card, .discover-card, .card').forEach((el, i) => {
            el.style.cssText += `opacity:0;transform:translateY(18px);transition:opacity 0.45s ease ${Math.min(i * 0.04, 0.4)}s,transform 0.45s ease ${Math.min(i * 0.04, 0.4)}s`;
            observer.observe(el);
        });
    </script>

    @functions {
        string FormatNum(int n)
        {
            if (n >= 1_000_000) return $"{n / 1_000_000.0:F1}M";
            if (n >= 1_000) return $"{n / 1_000.0:F0}K";
            return n.ToString();
        }
        string GetTimeAgo(DateTime dt)
        {
            var span = DateTime.UtcNow - dt;
            if (span.TotalMinutes < 60) return $"{(int)span.TotalMinutes}m ago";
            if (span.TotalHours < 24) return $"{(int)span.TotalHours}h ago";
            if (span.TotalDays < 7) return $"{(int)span.TotalDays}d ago";
            return dt.ToString("MMM dd");
        }
    }

</body>
</html>